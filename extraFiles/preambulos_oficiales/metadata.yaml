# Shared metadata for every subject built with estilo.latex.
#
# Keep here only what is the same for all of them. Anything that names a
# single subject belongs in its own src/00_portada.md front matter, which
# pandoc reads after this file and therefore wins.
#
# The template reads `subject`, `type` and `about`, and every subject
# declares those three in its portada. The fields below are not read by the
# template today; they stay because they describe the whole collection and
# any future template addition would want them.
#
# @author Ismael Sallami Moreno

author: "Ismael Sallami Moreno"
university: "Universidad de Granada"
lang: es
fontsize: 12pt
